{
  "name": "corporate-carpool",
  "version": "1.0.0",
  "description": "Corporate Carpool — full-stack app (Express + Socket.io + MongoDB Atlas)",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "npm --prefix server install --omit=dev",
    "start":       "node server/server.js",
    "dev":         "npm --prefix server run dev",
    "test":        "npm --prefix server test"
  }
}
